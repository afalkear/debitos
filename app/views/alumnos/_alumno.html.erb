<% if alumno.observations.blank?%>
  <tr>
<% else %>
  <tr class="warning">
<% end%>
  <td><%= check_box_tag "alumno_ids[]", alumno.id %></td>
  <td><%= link_to "#{alumno.full_name}", "http://crm.padm.am/contacts/" + alumno.padma_id.to_s %></td>
  <td><%= best_in_place [@account,alumno], :identifier, classes: "forum" %></td>
  <% if false %>
    <td><%= best_in_place [@account,alumno], :bill, classes: "forum" %></td>
  <% end %>
  <% if alumno.secret.nil? %>
    <td><%= best_in_place [@account,alumno], :secret, :nil => "Ingrese número de tarjeta" %></td>
  <% else %>
    <td><%= best_in_place [@account,alumno], :secret,
                        classes: "forum alumno_secret",
                        display_as: :number_for_humans %></td>
  <% end %>
<td><%= best_in_place [@account,alumno], :card_company_id, type: :select, collection: @account.responsibles.first.card_companies.map{|cc| [cc.id, cc.description]} %></td>
  <td><%= best_in_place [@account,alumno], :card_type, type: :select, collection: [["credito", "credito"], ["debito", "debito"]], classes: "forum" %></td>
  <td><%= alumno.plan%></td>
  <td><%= best_in_place [@account,alumno], :amount, classes: "forum" %></td>
  <td><%= best_in_place [@account,alumno], :payed, type: :checkbox, collection: %w[No Si], classes: "forum" %></td>
  <td><%= alumno.observations%></td>
  <td>
    <%= link_to [@account,alumno], method: :delete, data: { confirm: "You sure?" } do %>
      <i class="icon-trash"></i>
    <% end %>
  </td>
</tr>
