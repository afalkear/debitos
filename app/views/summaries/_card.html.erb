<h3><%= card %></h3>
<h5><%= type %></h5>
<% if current_user.card_companies && !current_user.card_companies.where(name:card.downcase).empty? %>
    Establecimiento: <%= current_user.card_companies.where(name: card.downcase).first.establishment %>
<% else %>
    No se tiene número de establecimiento para esta empresa, genere una en
    <%= link_to "Agregar un nuevo establecimiento", edit_user_path(current_user) %>
<% end %>
<br>
<table class="alumnos table table-hover table-condensed">
    <tr>
      <th>Nombre</th>
      <th>Nro Tarjeta</th>
      <th>Identificador</th>
      <th>Factura</th>
      <th>Compa&ntilde;&iacute;a</th>
      <th>Tipo</th>
      <th>Plan</th>
      <th>Monto</th>
      <th>Nuevo?</th>
      <th>Pago?</th>
      <th>Observaciones</th>
      <th>Borrar</th>
    </tr>
    <%= render :partial => "single_card", :collection => alumnos, :as => :alumno %>
  </table>
Alumnos: <%= alumnos.count %><br />
Total: <%= alumnos.sum("amount") %><br />
<%= link_to "Generar resumen", "#{card.downcase}"+"_"+"#{type.downcase}", class: "btn btn-large btn-primary" %>
<%= link_to "Download", download_summaries_path %>