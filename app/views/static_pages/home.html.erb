<% content_for :sidebar do %>
    <%= render "main_sidebar" %>
<% end %>
<div class="center hero-unit">
  <h1>Sistema de d&eacute;bitos</h1>

  <h2>
    Para empresas del 
    <a href="http://metododerose.org/">M&eacute;todo DeRose</a>
    de Argentina.
  </h2>

  <% if signed_in? %>
    <%= link_to "Entrar", users_path(current_user.id), class: "btn btn-large btn-primary" %>
  <% else %>
    <%= link_to "Registrate!", signup_path, class: "btn btn-large btn-primary" %>
  <% end %>
</div>